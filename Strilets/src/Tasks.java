import java.util.Date;
import java.util.Set;
import java.util.SortedMap;

/**
 * Created by Valera on 29.05.2016.
 */
public class Tasks {
   /* SortedMap<Date, Set<Task>> calendar(Iterable<Task> tasks, Date start, Date end){
        for(int i = tasks.le ; i > 0 ; i--){
            for(int j = 0 ; j < i ; j++){
            if( arr[j] > arr[j+1] ){
                int tmp = arr[j];
                arr[j] = arr[j+1];
                arr[j+1] = tmp;
            }
        }
    }

        return null;
    } */
}
